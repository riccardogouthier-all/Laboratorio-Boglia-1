# CI del Portale - i GATE DI QUALITA.
# Gira su ogni pull request: se un gate rosseggia, quel codice non entra in main.
name: CI

on:
  pull_request:
  push:
    branches-ignore: [main]

permissions:
  contents: read

env:
  # Le regole del capitolato che la pipeline fa rispettare.
  GATE_CHECKS: "CKV_AWS_18,CKV_AWS_19,CKV_AWS_20,CKV_AWS_21,CKV_AWS_28,CKV_AWS_119,CKV_AWS_7,CKV_AWS_53,CKV_AWS_54,CKV_AWS_55,CKV_AWS_56,CKV2_AWS_6,CKV_ITS_1"
  WORKDIR: "CORSO_AUTOMATION_PIPELINE/Portale"

jobs:

  # ---------- Gate 0: il gate che controlla che i gate ci siano ----------
  rilevamento:
    name: Struttura del progetto
    runs-on: ubuntu-latest
    outputs:
      cfn: ${{ steps.binario.outputs.cfn }}
      tf: ${{ steps.binario.outputs.tf }}
    steps:
      - uses: actions/checkout@v4

      - name: Quale binario di infrastruttura c'e in questo repo?
        id: binario
        run: |
          BASE="CORSO_AUTOMATION_PIPELINE/Portale"
          if [ -f "$BASE/infra/portale-its.yaml" ]; then echo "cfn=true" >> "$GITHUB_OUTPUT"; else echo "cfn=false" >> "$GITHUB_OUTPUT"; fi
          if [ -f "$BASE/infra/tf/main.tf" ];      then echo "tf=true"  >> "$GITHUB_OUTPUT"; else echo "tf=false"  >> "$GITHUB_OUTPUT"; fi
          if [ ! -f "$BASE/infra/portale-its.yaml" ] && [ ! -f "$BASE/infra/tf/main.tf" ]; then
            echo "::error::Nessun file di infrastruttura: qualcuno ha cancellato il codice IaC."
            exit 1
          fi
          echo "trovato:"; ls -1 "$BASE/infra" "$BASE/infra/tf" 2>/dev/null || true

  # ---------- Gate 1: l'applicazione ----------
  applicazione:
    name: App - install, test, build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "CORSO_AUTOMATION_PIPELINE/Portale"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Installazione bloccata dal lockfile
        run: npm ci

      - name: Test
        run: npm test

      - name: Build del sito
        run: npm run build

      - name: Nessun segreto nel sito pubblicato
        run: bash "CORSO_AUTOMATION_PIPELINE/Portale/script/sniffa-segreti.sh" "CORSO_AUTOMATION_PIPELINE/Portale/dist"

      - uses: actions/upload-artifact@v4
        with:
          name: portale-dist
          path: "CORSO_AUTOMATION_PIPELINE/Portale/dist/"
          retention-days: 7

  # ---------- Gate 2: nessun segreto nel repository ----------
  segreti:
    name: Segreti
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Sniffer fatto in casa (tre righe di grep)
        run: bash "CORSO_AUTOMATION_PIPELINE/Portale/script/sniffa-segreti.sh" .

      - name: Scansione con checkov
        run: |
          pip install --quiet checkov
          checkov -d . --framework secrets --enable-secret-scan-all-files \
            --skip-path node_modules --skip-path dist --compact

  # ---------- Gate 3A: infrastruttura CloudFormation ----------
  infra-cloudformation:
    name: IaC - CloudFormation
    needs: rilevamento
    if: needs.rilevamento.outputs.cfn == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Strumenti
        run: pip install --quiet cfn-lint checkov

      - name: Sintassi e buone pratiche del template
        run: cfn-lint "CORSO_AUTOMATION_PIPELINE/Portale/infra/portale-its.yaml"

      - name: Regole del capitolato (policy as code)
        run: |
          checkov -f "CORSO_AUTOMATION_PIPELINE/Portale/infra/portale-its.yaml" \
            --external-checks-dir "CORSO_AUTOMATION_PIPELINE/Portale/policy" \
            --check "$(echo $GATE_CHECKS | tr -d ' ')" \
            --compact

      - name: Audit completo (informativo, non blocca)
        run: checkov -f "CORSO_AUTOMATION_PIPELINE/Portale/infra/portale-its.yaml" --compact --soft-fail

  # ---------- Gate 3B: infrastruttura Terraform ----------
  infra-terraform:
    name: IaC - Terraform
    needs: rilevamento
    if: needs.rilevamento.outputs.tf == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Terraform
        run: |
          curl -sSLo tf.zip https://releases.hashicorp.com/terraform/1.12.0/terraform_1.12.0_linux_amd64.zip
          sudo unzip -oq tf.zip -d /usr/local/bin
          rm tf.zip && terraform version

      - name: Formattazione
        working-directory: "CORSO_AUTOMATION_PIPELINE/Portale/infra/tf"
        run: terraform fmt -check -recursive

      - name: Inizializzazione e validazione
        working-directory: "CORSO_AUTOMATION_PIPELINE/Portale/infra/tf"
        run: |
          terraform init -input=false -backend=false
          terraform validate

      - name: Regole del capitolato (policy as code)
        run: |
          pip install --quiet checkov
          checkov -d "CORSO_AUTOMATION_PIPELINE/Portale/infra/tf" --framework terraform \
            --external-checks-dir "CORSO_AUTOMATION_PIPELINE/Portale/policy" \
            --check "$(echo $GATE_CHECKS | tr -d ' ')" \
            --compact

      - name: Audit completo (informativo, non blocca)
        run: checkov -d "CORSO_AUTOMATION_PIPELINE/Portale/infra/tf" --framework terraform --compact --soft-fail
